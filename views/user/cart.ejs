<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    


    <table>
        <thead>
          <tr>
            <th>Product Name</th>
            <th>Price</th>
            <th>Quantity</th>
            <th>Total</th>
          </tr>
        </thead>
        <tbody>
          <% for (let i = 0; i < cartItems.length; i++) { %>
            <tr>
              <td><%= cartItems[i].product.name %></td>
              <td>$<%= cartItems[i].product.price %></td>
              <td><%= cartItems[i].quantity %></td>
              <td>$<%= cartItems[i].product.price * cartItems[i].quantity %></td>
            </tr        

            >%}
        </tbody>

     </table>

</body>

</html>